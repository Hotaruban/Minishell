			printf("token count = %d \n", tokens->token_count);
		for (int i = 0; i < tokens->token_count; i++)
			printf("token[%d] = %s \n", i, tokens->tokens[i]);
	printf("cmd[%d].cmd[%d] = %s", i , j, data->cmd[i].cmd[j])
	// printf("cmd.cmd[0] = %s\n", data->cmd[0].cmd[0]);

	git push origin wolf_branch_42

	minishell-hh$ $HOME
execve failed